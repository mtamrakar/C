----------------------------------------------------------------------
--test est test
----------------------------------------------------------------------

CREATE OR REPLACE FUNCTION sand_box.gittest()
  RETURNS BOOLEAN AS
$BODY$
BEGIN 
	RETURN TRUE;
END;
$BODY$
  LANGUAGE plpgsql VOLATILE
  COST 100;
