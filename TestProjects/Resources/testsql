----------------------------------------------------------------------
--test est test
----------------------------------------------------------------------

CREATE OR REPLACE FUNCTION sandbox.gittest()
  RETURNS BOOLEAN AS
$BODY$
BEGIN 
	RETURN FALSE;
END;
$BODY$
  LANGUAGE plpgsql VOLATILE
  COST 100;
